/*
 * IMX415.cpp
 *
 * Adapted from the original OV5640.cpp - the driver is header-only
 * (templates/inline definitions live in IMX415.h); this translation unit
 * exists so the class has an out-of-line home and to keep the same file
 * layout as the original Pcam-5C/OV5640 project.
 */

#include "IMX415.h"

namespace digilent {


} /* namespace digilent */
